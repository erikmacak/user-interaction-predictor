[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "predictor-backend"
version = "0.1.0"

[tool.setuptools]
packages = ["api", "core", "domain", "schemas", "services"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]